<%= form_for(@micropost) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
<%= f.text_area :content, placeholder: "Write details of your errand here" %>
</div>
<%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
